// Progressive enhancement for the landing page.
// The page is fully functional without JS: the load reveal is CSS-only and
// respects prefers-reduced-motion. This hook is a place for future niceties
// (e.g. a theme toggle) without blocking first paint.
(() => {
  "use strict";
  // Intentionally minimal — no client-side logic required for the landing.
})();
